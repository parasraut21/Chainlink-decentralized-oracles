{
  "name": "chainlink-price-feed-contract",
  "license": "MIT",
  "version": "1.0.0",
  "description": "A Chainlink price feed consumer contract that fetches latest price data from decentralized oracles.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "lint": "solhint 'contracts/*.sol'",
    "lint:js": "echo 'JavaScript linting placeholder - consider adding ESLint'",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@chainlink/contracts": "1.5.0",
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.22.3",
    "solhint": "^3.3.7"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
